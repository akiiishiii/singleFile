// s4_2.cpp
#include <iostream>

int main(int argc, char const *argv[]) {
    int a, b, c, gcd;
    std::cout << "Input three number: ";
    std::cin >> a >> b >> c;
    for (gcd = a; gcd > 1; gdc--)
        if (!(a % gcd + b % gcd + c % gcd))
            break;
    std::cout << gcd << '\n';
    return 0;
}
